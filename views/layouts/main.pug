doctype html
head
  meta(charset='UTF-8')
  title Deli Health
  link(rel="stylesheet", href="https://bootswatch.com/4/minty/bootstrap.min.css")
body
    //- if (!user)
    //-     a(href="/signin") Signin
    //-     a(href="/signup") Signup
    //- else
    //-     a(href="/logout") Logout
    //-     a(href="/profile") Profile

    nav(class="navbar navbar-expand-lg navbar-dark bg-primary")
        a(class="navbar-brand" href="/Bienvenido") Deli Health
        button(class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
            span(class="navbar-toggler-icon")
        div(class="collapse navbar-collapse" id="navbarNav")
            ul(class="navbar-nav ml-auto" )
                if(user)
                    li.nav-item.active
                        a.nav-link(href='/profile') Perfil
                    li.nav-item
                        a.nav-link(href='logout') Salir
                else
                    li.nav-item.active
                        a.nav-link(href='/signin')  Iniciar Sesión 
                    li.nav-item
                        a.nav-link(href='signup') Registrarse
br
br

div(class='container')
    if(mensajedeinicio)
        a(class='alert alert-danger') #{mensajedeinicio}
        br
        br

    if(mensajederegistro)
        a(class='alert alert-danger') #{mensajederegistro}